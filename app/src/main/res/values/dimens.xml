<resources>

    <style name="ItcMaterialDesignTheme" parent="AppTheme">
        <!-- customize theme for older device -->
    </style>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Followings are applicable for apps accross API levels; new or old. -->
        <item name="colorPrimary">@color/cardview_dark_background</item>
        <item name="colorPrimaryDark">@color/cardview_light_background</item>
        <item name="colorAccent">@color/cardview_shadow_start_color</item>
    </style>

</resources>